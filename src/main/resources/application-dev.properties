# Development Profile Configuration

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/tiling?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=ChogyalWp
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway for MySQL
spring.flyway.locations=classpath:db/migration/mysql

# Mock mail for development
spring.mail.host=localhost
spring.mail.port=1025

# Verbose logging for development
logging.level.com.tilingroofing=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE

# Development CORS - allow all localhost origins
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173,http://localhost:8082
app.admin.email=dev-admin@localhost

# Google OAuth2 Configuration (Development)
# This is used to verify Google ID tokens from the frontend
app.google.client-id=${GOOGLE_CLIENT_ID:392705207882-qmru3mi3bp7hv675el1b5vm0eh4ob3a5.apps.googleusercontent.com}

# JWT Configuration
# IMPORTANT: Use a strong random secret (minimum 32 characters / 256 bits)
# Generate with: openssl rand -base64 32
app.jwt.secret=${JWT_SECRET:dev-jwt-secret-key-change-in-production-minimum-256-bits-required-for-security}
app.jwt.expiration=${JWT_EXPIRATION:3600000}
